package TipiGenerici;

public class Main {
    public static void main(String[] args) {
        try{
            /*
            Box<Integer> b0 = new Box<>();
            b0.setT(2);

            Box<Integer> b1 = new Box<>(10);
            Box<Double> b2 = new Box<>(5.50);
            Box<String> b3 = new Box<>("testo");
            Box<Boolean> b4 = new Box<>(true);

            Box<Integer> b5 = new Box<>(null);

            System.out.println(b0.toString());
            System.out.println(b0.tipo());

            System.out.println(b1.toString());
            System.out.println(b1.tipo());

            System.out.println(b2.toString());
            System.out.println(b2.tipo());

            System.out.println(b3.toString());
            System.out.println(b3.tipo());

            System.out.println(b5.toString());
            System.out.println(b5.tipo());

            // Istanzio un oggetto di classe persona e verifico che Box funzioni
            Persona p = new Persona("cognome", "nome");
            Box<Persona> b6 = new Box<>(p);
            System.out.println(Box.verificaTipo(b6));

            // Verifico di che tipo è la classe del parametro box
            System.out.println(Box.verificaTipo(b1));

            // Nel replit condiviso ci sono altri esempi di dichiarazione
            // e stampa di oggetti di tipi diversi

            Integer n = 10;
            System.out.println(Stampa.stampaElemento(n));
             */
        }catch (Exception e){
            System.out.println(e.getMessage());
        }
    }
}
